@page "{id}"
@model Reserve.Pages.Appointment.CalendarNotificationModel

<h1 class="notification-title mb-lg-5">Calendar Created Successfully!</h1>
<div class="d-flex flex-column justify-content-center align-items-center">
    <div class="notification-border mb-lg-5">
        <div>
            <p class="notification-text calendar-notification d-flex justify-content-center mb-1">You can share your calendar &nbsp; <a href="@Url.Page("ReserveAppointment", new {id = Model.Id})">here</a></p>
        </div>
        <div>
            <p class="notification-text calendar-notification d-flex justify-content-center mb-1">You can view appointments at this &nbsp; <a href="@Url.Page("EditSlots", new {id = Model.Id})">link</a></p>
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <a href="/" class="btn btn-custom reserve-blue-button">Home Page</a>
    </div>
</div>
